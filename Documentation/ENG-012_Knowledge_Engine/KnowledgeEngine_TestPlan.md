# Knowledge Engine Test Plan

The isolated suite covers public contract, lifecycle, configuration, complete immutable model, conservative no-inference generation, summaries, both lookup identities, all search dimensions, relationship preservation, statistics, JSON serialization, deterministic checksums/integrity, legacy migration, atomic storage, rebuild, invalid semantic input, concurrency, 1,000-record performance, structured logging/failure, and Rule 40 source imports.

Integration starts the twelve-Engine Composition Root, validates registry/health/integrity, exercises all read-only routes, builds the frontend, and runs every ENG-001–012, launcher, Object Library compatibility, and session lifecycle regression suite.
