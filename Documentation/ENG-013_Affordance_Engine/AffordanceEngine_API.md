# Affordance Engine API

Contract `taskgraph.affordance` 1.0.0 exposes lifecycle, all/ID/Object lookup, object/capability/action search, export, integrity validation, and statistics.

HTTP: `GET /affordances`, `/affordances/{id}`, `/affordances/search?q=&capability=&action=`, `/affordances/actions`, `/affordances/statistics`. No mutation route exists.
