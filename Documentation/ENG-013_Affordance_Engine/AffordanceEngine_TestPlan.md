# Affordance Engine Test Plan

Validate contract/lifecycle/model/immutability, every rule and union, unknown no-guess behavior, rule version, ID/Object lookup, searches, action categories/statistics, serialization/checksums, legacy migration, atomic storage, rebuild, bad source/config/logging, 20-thread reads, 1,000-record performance, Rule 40, thirteen-Engine startup/health/shutdown, every API, TypeScript/Vite/ESLint, and all ENG-001–013 plus launcher/session regressions.
