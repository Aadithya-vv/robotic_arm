# TaskGraph Desktop Application

The v0.1 desktop shell uses Python Tkinter and reads only composed public Engine contracts. It owns presentation, live status polling, validation display, report export, and the user-triggered shutdown action. It contains no Engine business logic.
