# Validation

Validation checks non-empty plans, unique node identities, valid edge references, node checksums, plan checksum, and current Affordance support for every action. Tests cover deterministic ordering, rejection, lookup/search/statistics/export, persistence/reload/import, tamper detection, lifecycle, and storage isolation.

