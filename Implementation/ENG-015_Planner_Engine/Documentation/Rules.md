# Planning Rules

Rules are exact goal-to-action declarations in `rules.py`. Version 1.0.0 provides Pour Water, Stir, and Fill Container. A plan is created only when one current Affordance Record supplies every action in the selected rule. Rule changes require a version change and regression tests.

