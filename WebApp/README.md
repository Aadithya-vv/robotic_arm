# TaskGraph v1.0 Web Workstation

React + TypeScript + Vite presentation for the unchanged TaskGraph Python runtime.

```powershell
cd WebApp
npm install
npm run dev
```

The Vite server proxies REST and WebSocket traffic to `http://127.0.0.1:8000`.

Production validation:

```powershell
npm run build
```
